{
  "name": "dictafono",
  "version": "1.0.0",
  "displayName": "Dictafono",
  "private": true,
  "description": "Aplicación de dictado voz a texto (Cordova wrapper)",
  "license": "MIT",
  "scripts": {
    "prepare:www": "rm -rf www && mkdir -p www && cp -r index.html styles.css manifest.json script_limpio.js www/",
    "build:cordova": "npm run prepare:www && cordova platform rm android || true && cordova platform add android@12 && cordova build android --release"
  },
  "cordova": {
    "plugins": {
      "cordova-plugin-speechrecognition": {},
      "cordova-plugin-android-permissions": {}
    },
    "platforms": []
  },
  "dependencies": {
    "cordova-android": "^12.0.0"
  },
  "devDependencies": {}
}
